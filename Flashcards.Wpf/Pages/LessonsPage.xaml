<Page x:Class="Flashcards.Wpf.LessonsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Flashcards.Wpf"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="800"
      Title="LessonsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding Lessons}" Width="300">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:Lesson/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="1" Margin="0 5">
            <TextBlock Margin="3" FontSize ="16" >Lesson name:</TextBlock>
            <TextBox Margin="3" FontSize ="16" Text="{Binding NewLessonName, Mode=TwoWay}"></TextBox>
            <Button Margin="3" FontSize ="16" Command="{Binding AddNewLessonCommand}">Add lesson</Button>
            <Button Margin="3" FontSize ="16" >Delete selected lessons</Button>
        </StackPanel>
    </Grid>
</Page>
